/**
 * @heiyun/cli — index.ts（包入口）
 * ===============================
 * CLI 包的桶文件。
 * 当前为空导出（{}），实际入口是 bin/heiyun.js → dist/main.js。
 * 这是 npm 包的标准结构：index.ts 作为库入口，main.ts 作为 CLI 入口。
 */

// @heiyun/cli — CLI 入口 & TUI
export {};
