{
  "name": "heiyun-code",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/ai",
    "packages/tools",
    "packages/agent-core",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "clean": "rm -rf packages/*/dist"
  },
  "engines": {
    "node": ">=20"
  }
}
